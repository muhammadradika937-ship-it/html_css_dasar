let rTabung = 7;
let tTabung = 10;

let volumeTabung = Math.PI * rtabung * rTabung * tTabung;
let luasPermukaanTabung = 2 * Math.PI * rTabung * (rTabung + tTabung);

console.log("Luas Permukaan:", volumeTabung.toFixed(2));
console.log("Luas Permukaan Tabung:", luasPermukaanTabung.toFixed(2));